/** User-visible copy for the main-thread private-draft composer (ui-features §9). */

export const PRIVATE_BRANCH_LEAD = "Private draft";

/** Shown next to the checkbox (short line). */
export const PRIVATE_BRANCH_DESCRIPTION =
  "Only you see this user message on the branch under the selected node until you continue on a shared path others can follow.";

/** Native tooltip on the whole control (slightly longer). */
export const PRIVATE_BRANCH_LABEL_TITLE =
  "When checked, your next send stores a private user line under the selected tree node. Collaborators do not see it until you move forward on a shared branch (see repo docs for branching semantics).";
