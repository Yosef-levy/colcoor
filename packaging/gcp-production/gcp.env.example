# GCP provisioning config for Colcoor multi-VM production.
# Copy to gcp.env, edit planned resource names, then run: ./scripts/create-shared-env.sh
# create-shared-env.sh creates Cloud SQL, Memorystore, and GCS if they do not exist yet.
#
# Requires: gcloud CLI authenticated to COLCOOR_GCP_PROJECT.

COLCOOR_GCP_PROJECT=your-gcp-project-id
COLCOOR_GCP_REGION=us-central1
COLCOOR_GCP_ZONE=us-central1-a
COLCOOR_GCP_NETWORK=default

# Planned managed resource names (created in Step 5 if missing)
COLCOOR_CLOUDSQL_INSTANCE=colcoor-prod
COLCOOR_REDIS_INSTANCE=colcoor-redis
COLCOOR_GCS_BUCKET=your-project-colcoor-images
COLCOOR_LB_NAME=colcoor-api-lb

# Cloud SQL (db-custom-* tiers need ENTERPRISE edition; db-perf-optimized-* use ENTERPRISE_PLUS)
COLCOOR_CLOUDSQL_TIER=db-custom-2-7680
# COLCOOR_CLOUDSQL_EDITION=ENTERPRISE
COLCOOR_CLOUDSQL_VERSION=POSTGRES_16
COLCOOR_POSTGRES_DB=colcoor
COLCOOR_POSTGRES_USER=colcoor

# Comma-separated GCE VM names (same zone) for load balancer registration.
COLCOOR_API_VM_INSTANCES=colcoor-api-1,colcoor-api-2
COLCOOR_API_VM_TAG=colcoor-api

COLCOOR_API_REPLICAS=2
WEB_CONCURRENCY=2

# Optional: VM service account for GCS (default: first API VM's attached SA).
# COLCOOR_GCS_SERVICE_ACCOUNT=colcoor-api@project.iam.gserviceaccount.com
